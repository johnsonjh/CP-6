!DEFAULT SIACCT=:XSI,GNACCT=:XSI,LBACCT=:LIBRARY, XACCT=:XSI
!JOB  NAME=WEBSTER,WSN=LOCAL
!RESOURCE TIME=5,MEM=256,NHPSN,ACC=(.SIACCT,.GNACCT,.LBACCT)
!ROUTE Jerry Waldorf, LADC
!"
!"  By default, the output created by this job is EDGEMARKed.
!"  However, if you do NOT want it EDGEMARKed, do a DEST=LP substitution on
!"  your BATCH or XEQ command such as:
!"    !BATCH WEBSTER_CRU.XACCT DEST=LP
!"
!DEFAULT DEST=*LS
!IF $FID_EXIST('TO.:SYS') & $MODE='BATCH' THEN TO AC=%$ACCT Your WEBSTER compile just started.
!LET LISTING = 'DEST'
!LET MONNUM=$SUBSTR($DATE,2,2)
!LET MONTHS='JanFebMarAprMayJunJulAugSepOctNovDec'
!LET RIGHT=$SUBSTR(MONTHS,(MONNUM*3)-3,3) || ' ' || $SUBSTR($DATE,4,2)
!IF $SUBSTR(RIGHT,4,1) ~= '0' THEN GOTO DATE_OK
!LET RIGHT= $SUBSTR(RIGHT,0,4) || $SUBSTR(RIGHT,5,1)
!DATE_OK:
!PL6 WEBSTER_SI61.SIACCT OVER *WEBSTER_OU6.GNACCT,DEST (NSYS,LS,BC,OU,SR(.SIACCT,.LBACCT))
!PARTRGE WEBSTER_SIN1.SIACCT INTO *WEBSTER_OU6.GNACCT,DEST (LS,OU,SR(.SIACCT,.LBACCT))
!IF LISTING = 'LP' THEN GOTO DONT_EDGEMARK
!EDGEMARK.XACCT DEST TO LP@LOCAL(F='PERF',L='WEBSTER.X',R='%RIGHT')
!DONT_EDGEMARK:
!LINK B_COPYRIGHT.LBACCT, ;
      *WEBSTER_OU6.GNACCT ;
 OVER WEBSTER.GNACCT(SIDCB=F$DICT,UIDCB=M$SI,LODCB=M$LO)
!IF $FID_EXIST('TO.:SYS') & $MODE='BATCH' THEN TO AC=%$ACCT Your WEBSTER compile just finished.
