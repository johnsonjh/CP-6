*  Maximum routines a program can ref
      CONSTANT $TABLESIZE = 3000
*  Maximum refs all routines can have
      CONSTANT $MAXREF = 100000
*  Maximum routines a program can def
      CONSTANT $MAXOBJ = 4000
*  Maximum nodes an overlay can have
      CONSTANT $MAXNODE = 10000
*
      INTEGER NAMENUM, NAMESIZE ($TABLESIZE)
      CHARACTER NAME ($TABLESIZE) * 31
      COMMON /HASHV/ NAMENUM, NAMESIZE
      COMMON /HASHC/ NAME
*
      CONSTANT    FALSE=0, TRUE=1, DOUBLE=2, ENTRY=3
      INTEGER DEFED ($TABLESIZE)
      INTEGER REF (0:$MAXREF)
      INTEGER NREF (0:$TABLESIZE)
      INTEGER SREF (0:$TABLESIZE)
      INTEGER NEXTREF
      INTEGER PRIME ($TABLESIZE)
      COMMON /DEFV/ DEFED, REF, PRIME, NREF, SREF, NEXTREF
*  Used by PRINTTREE
      INTEGER CALLPATH (0:200), MAXDEPTH, INDENTMULTIPLE
      LOGICAL RECURSE ($TABLESIZE)
      COMMON /PTREEV/ CALLPATH, MAXDEPTH, INDENTMULTIPLE, RECURSE
 
*  Options
      LOGICAL $CALL TREE, $SIZES, $WHO CALLS WHO,
     *   $WHO WHO CALLS, $NAMES, $NOAUTO, $OVERLAY
      COMMON /OPTION/ $CALL TREE, $SIZES, $WHO CALLS WHO,
     *   $WHO WHO CALLS, $NAMES, $NOAUTO, $OVERLAY
*  Include file accounts to search
      INTEGER NACCT
      COMMON /NACCOUNT/ NACCT
      CHARACTER ACCT(0:7)*8
      COMMON /CACCOUNT/ ACCT
